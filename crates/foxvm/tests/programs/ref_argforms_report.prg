* PRTINFO's argument forms a golden had not yet called: asked about a named printer rather
* than the default one. A printer's real names are the host's, not the language's, and differ
* by machine, so what is measured is the one answer that is the same everywhere - a printer
* nothing has installed answers -1, the reference's own word for a setting that is not there.
? PRTINFO(1, "NoSuchPrinterXYZ123")

* COVERS: PRTINFO
